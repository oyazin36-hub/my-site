# 相見積もり比較アプリ — Windows 説明書

複数社の見積書PDFから **品番** と **単価** を自動で読み取り、
**単価 × 倍率（100円単位で切り上げ）** で各社を比べて、A4で印刷できるアプリです。

人が入力するのは **社名** と **倍率** だけです。

---

## はじめに：用意するもの（最初の1回だけ）

このアプリを動かすには、パソコンに次の**2つ**を入れます。1回入れれば、次からは不要です。

### ① Python（パイソン）
1. https://www.python.org/downloads/ を開く
2. 黄色い「**Download Python**」ボタンでインストーラをダウンロード
3. ダウンロードしたファイルを実行
4. ⚠️ **最初の画面の一番下「Add Python to PATH」に必ずチェック** を入れる
5. 「Install Now」をクリック → 完了

### ② Tesseract（テッセラクト＝文字を読み取る部品）日本語入り
1. https://github.com/UB-Mannheim/tesseract/wiki を開く
2. 「**tesseract-ocr-w64-setup-…….exe**」（64bit版）をダウンロード
3. 実行して進める。途中の **「Additional language data」→「Japanese」にチェック** を入れる
   （これを忘れると日本語が読めません）
4. インストール先は **そのまま（変更しない）** でOK
   → 既定の `C:\Program Files\Tesseract-OCR` に入れば、アプリが自動で見つけます

> 💡 すでに別の場所に入れている場合は、`tesseract.exe` がある場所を
> 環境変数 `TESSERACT_CMD` に設定すると、そこを使います（上級者向け）。

---

## アプリの起動（毎回これだけ）

1. このアプリのフォルダを開く
2. **「起動.bat」をダブルクリック**
3. 黒い画面が出て、初回は準備が走ります（数分かかることがあります）
4. しばらくすると **ブラウザが自動で開きます**
   - 開かないときは、ブラウザのアドレス欄に `http://127.0.0.1:5000` と入力

> ⚠️ 黒い画面は**閉じないでください**（閉じるとアプリが止まります）。

---

## 使い方

1. **① 見積書PDFをアップロード**
   各社のPDFを選びます（複数まとめて選べます）。スキャン画像のPDFでもOK。
   「読み取る」を押すと自動で品番・単価を読み取ります（数十秒）。

2. **② 社名と倍率を入力**
   各社の「社名」と「倍率」を入れます。倍率を変えると比較表がその場で再計算されます。

3. **③ 比較表を確認**
   - 単価 × 倍率（**100円単位で切り上げ**）が出ます
   - 各品番の **最安値が緑** で光ります
   - 読み取りの信頼度マーク
     - **◎** … 安心（複数回読んで一致）
     - **○** … 2回一致
     - **△ / 要確認（黄色）** … 念のため確認

4. **④ 要確認だけチェック**
   黄色いセルにマウスを乗せると、**PDFの原本画像**が出ます。
   数字が合っていればそのまま、違えば **セルをクリックして手入力** で直せます。

5. **⑤ A4で印刷**
   「🖨 A4で印刷」ボタン → 用紙サイズ **A4** を選んで印刷します。

---

## 単価列を手で指定したいとき（自動がズレた場合）

各社カードの「**単価列を指定して読み直す**」ボタンを押すと、
PDFに列の区切りが重なって表示されます。「単価」の列をクリックして
「この列で読み直す」を押すと、その社だけ読み直します。
（ふだんは自動でうまくいくので、使わなくてOKです）

---

## 終わるとき
黒い画面で **Ctrl + C** を押すか、**×ボタン**で閉じます。

---

## 困ったとき

| 症状 | 対処 |
|---|---|
| 黒い画面に「Python が見つかりません」 | ①のPythonを入れる。**「Add Python to PATH」のチェック**を忘れていないか確認 |
| 読み取りで「tesseract …」のエラー | ②のTesseractを入れる。**Japanese**にチェックを入れたか確認 |
| 日本語の品番が読めない／全部おかしい | Tesseractの **Japanese 言語データ** が入っていない可能性。②を入れ直す |
| ブラウザが開かない | 手動で `http://127.0.0.1:5000` を開く |
| 「ライブラリのインストールに失敗」 | インターネットに接続して、もう一度「起動.bat」を実行 |
| 数字が一部 △（要確認）になる | 仕様です。黄色いセルだけ原本と照合してください（値は概ね正しいです） |

---

## 読み取りの前提（参考）
- 単価は **10円きざみ**（末尾0）として読みます。
- 金額列（=単価×数量）は使いません。回答に使うのは **単価** だけです。
- 金額を扱うため、**△・要確認のセルは印刷前に原本と照合**してください。
